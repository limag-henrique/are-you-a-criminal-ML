# Second phase

This directory will contain second-phase code and public outputs. It must never
contain source images, source paths, personal names, URLs or individual
embeddings. The first implementation step is cross-fitted internal validity,
not a replacement of the primary analysis.
